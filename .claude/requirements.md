# Whale 프로젝트 요구사항 명세서

> 본 문서는 `/whale-app/`에 구현된 Whale 이미지 튜닝 스튜디오의 기능 요구사항(FR)과 비기능 요구사항(NFR)을 정리한 문서입니다.

## 표기 규약

### 우선순위 (MoSCoW)
- **MUST** — 필수. 미구현 시 제품 가치 상실
- **SHOULD** — 강력 권장. 1차 릴리즈 포함 권장
- **COULD** — 있으면 좋음. 후순위
- **WON'T** — 현재 범위 밖 (의도적 제외)

### 상태
- ✅ 구현됨
- 🟡 부분 구현 (UI만, 동작 mock)
- ⛔ 미구현
- 📌 기획만

### 기술 표기 원칙
- **행위**는 사용자 UX 관점에서 조건 → 액션 → 결과로 기술
- **측정 기준**: 객관적으로 평가 가능한 항목 체크리스트
- **알려진 제약**: 의도적 비지원 또는 현재 한계
- **평가 기준 없음**: 디자인 의도가 모호하거나 객관적 기준 정의 불가
- **구현 위치**: 프로젝트 루트(`/`) 기준 경로

---

## 1. 기능 요구사항 (FR)

### 1.1 Home 페이지

> **개요**: 사용자가 앱에 진입했을 때 가장 먼저 보는 대시보드. 인사 메시지, 진행 중인 작업 요약, 빠른 액션, 주간 통계, 최근 세션, 활동 로그를 한눈에 보여준다.
>
> **구현 위치**: `/whale-app/src/components/Home.tsx`

---

#### FR-HOME-01 — 인사 메시지 및 진행 요약 표시

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (정적 텍스트, 데이터 연동 없음) |
| **구현 위치** | `/whale-app/src/components/Home.tsx` |

##### 행위

**조건**: 사용자가 Home 페이지에 진입한다.

**액션**: 페이지가 로드된다.

**결과**:
1. 페이지 상단에 큰 글씨로 인사 메시지가 표시된다: `Good afternoon, Jiwon`
2. 인사 바로 아래 한 줄로 진행 상황 요약이 표시된다: `3 sessions in progress · 48 images tuned this week · 2 reviews awaiting`
3. 인사 메시지는 일반 본문 색, 진행 요약은 muted 색으로 차별 표시된다.

##### 측정 기준

- [ ] 페이지 진입 후 1초 이내에 인사 메시지와 요약이 모두 표시된다.
- [ ] 인사 메시지의 글자 크기가 28px 이상으로 시각적 위계가 가장 높다.

##### 알려진 제약

- ❌ 시간대(아침/점심/저녁)에 따라 인사말이 변하지 않음 — `Good afternoon` 고정.
- ❌ 사용자 이름(`Jiwon`)이 하드코딩되어 있음 — 인증/사용자 컨텍스트 미연동.
- ❌ 진행 요약 수치(3 / 48 / 2)가 정적 텍스트로 하드코딩되어 있음 — 실시간 데이터 연동 없음.

##### 평가 기준 없음

- 인사 메시지에 사용자 직군(엔지니어 등)을 반영해야 하는지에 대한 기준 없음.

---

#### FR-HOME-02 — 빠른 액션 카드

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (E2E/Library 카드만 동작, Upload는 UI만) |
| **구현 위치** | `/whale-app/src/components/Home.tsx` |

##### 행위

**조건**: 사용자가 Home 페이지에서 빠른 액션 영역을 본다.

**액션 1 — Open E2E Pipeline 카드 클릭**:
1. 화면이 즉시 E2E 페이지로 전환된다.
2. 상단 nav의 활성 탭이 [E2E]로 변경된다.

**액션 2 — Browse Library 카드 클릭**:
1. 화면이 즉시 Library 페이지로 전환된다.
2. 상단 nav의 활성 탭이 [Library]로 변경된다.

**액션 3 — Upload Reference 카드 클릭**:
- 시각적 피드백(hover, 클릭 누름)은 발생하지만, 페이지 전환이나 파일 업로드 다이얼로그가 나타나지 않는다.

**공통 시각 표현**:
- 각 카드에는 좌상단에 액센트 색상의 둥근 아이콘 박스가 표시된다 (E2E: pipeline, Library: folder, Upload: upload).
- 카드 제목, 설명 문구, 우하단에 키보드 단축키 칩(`⌘E` / `⌘L` / `⌘U`)이 표시된다.
- 카드에 마우스를 올리면 보더가 액센트 색으로 변하고, 카드 전체가 1px 위로 살짝 떠오른다.
- 3개 카드는 동일한 너비로 가로 정렬된다 (3-column grid).

##### 측정 기준

- [ ] E2E / Library 카드 클릭 시 1초 이내에 페이지가 전환된다.
- [ ] 카드 hover 시 보더 색 변경과 떠오름 효과가 즉시 적용된다 (지연 100ms 이내).
- [ ] 3개 카드의 폭이 정확히 동일하다 (반응형 그리드 깨짐 없음).

##### 알려진 제약

- ❌ 키보드 단축키 칩(`⌘E` / `⌘L` / `⌘U`)은 시각적 표시일 뿐 실제 키 입력으로 동작하지 않음.
- ❌ Upload Reference 카드는 클릭해도 동작 없음 — 파일 다이얼로그 또는 업로드 흐름 미구현.

##### 평가 기준 없음

- 카드 떠오름 애니메이션의 지속 시간 및 easing 곡선에 대한 명시 기준 없음.

---

#### FR-HOME-03 — 주간 통계 (Overview · this week)

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (정적 데이터) |
| **구현 위치** | `/whale-app/src/components/Home.tsx` |

##### 행위

**조건**: 사용자가 Home 페이지를 스크롤하거나 진입한다.

**액션**: 빠른 액션 카드 아래의 Overview 섹션이 표시된다.

**결과**:
1. 섹션 헤더에 `Overview · this week` 라벨과 우측에 모노스페이스로 `week 16 · Apr 13 – Apr 19` 기간 표시가 함께 나타난다.
2. 그 아래 가로 4-column 그리드로 통계 카드 4개가 표시된다:

   | 라벨 | 값 | 보조 정보 |
   |------|-----|----------|
   | Images tuned | `48` | `↑ 12 vs last week` (녹색) |
   | Active sessions | `3` | `night · auto · hdr-a` |
   | ΔE avg (target) | `2.14` | `target ≤ 3.0` |
   | Library | `1,284` | `420 ref · 864 sim` |

3. 통계 값은 큰 글씨(22px) 모노스페이스로, 라벨은 작은 대문자로 표시된다.
4. Images tuned 카드의 `↑ 12 vs last week`는 녹색(ok) 텍스트로 양적 증가를 시각적으로 강조한다.

##### 측정 기준

- [ ] 4개 카드의 폭이 정확히 동일하다.
- [ ] 통계 값은 모노스페이스 + 자릿수 정렬(tabular-nums)로 시각적으로 정렬된다.
- [ ] 카드 내부 패딩이 일관되어 텍스트가 카드 경계와 겹치지 않는다.

##### 알려진 제약

- ❌ 주간 통계 4개 모두 하드코딩 — 백엔드/계산 로직 미연동.
- ❌ 표시 기간(`week 16 · Apr 13 – Apr 19`)이 고정 텍스트 — 실제 현재 주차로 갱신되지 않음.
- ❌ 카드 클릭 시 상세로 이동하는 인터랙션 없음.
- ❌ ΔE avg가 target을 초과할 경우 경고 색으로 강조하는 동작 없음 (현재 항상 같은 색).

##### 평가 기준 없음

- 통계 카드를 4개 이상으로 확장할 때의 레이아웃 규칙 (몇 행으로 줄바꿈할지 등) 명시 기준 없음.

---

#### FR-HOME-04 — 최근 세션 (Recent sessions)

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (정적 카드, 모든 클릭이 동일하게 E2E로 이동) |
| **구현 위치** | `/whale-app/src/components/Home.tsx` |

##### 행위

**조건**: 사용자가 Home 페이지에서 Recent sessions 섹션을 본다.

**액션 1 — 섹션 진입**:
- 섹션 헤더에 `Recent sessions` 제목과 우측에 `View all >` 버튼이 표시된다.
- 그 아래 가로 3-column 그리드로 세션 카드 3개가 표시된다:

  | 카드 | 시간 | ΔE | 태그 |
  |------|------|-----|------|
  | Night scene · sensor-A v3 | 2 min ago | 2.8 | night, low-light, **active** |
  | HDR · outdoor portrait | 1h ago | 1.9 | hdr, skin, **active** |
  | ColorChecker SG · D65 | Yesterday | 1.2 | chart, calibration, **active** |

- 각 카드는 다음 구조로 표시된다:
  - 상단: 두 개의 reference / simulation 썸네일이 좌우로 분할된 미리보기 영역 (높이 150px)
  - 미리보기 하단에 어두운 그라데이션 오버레이
  - 중단: 세션 이름(좌, 잘리면 ellipsis) + ΔE 값(우, 모노스페이스)
  - 하단: 일반 태그들 + 액센트 색의 `active` 배지

**액션 2 — 카드 클릭**:
1. 카드(어느 카드든 동일)를 클릭하면 화면이 즉시 E2E 페이지로 전환된다.
2. 상단 nav가 [E2E]로 변경된다.

**액션 3 — View all 버튼 클릭**:
- 시각적 피드백만 있고 페이지 전환이나 모달 표시 등의 동작이 발생하지 않는다.

**액션 4 — 카드 hover**:
- 카드 보더가 강조 색(`--border-strong`)으로 변경된다.

##### 측정 기준

- [ ] 카드 클릭 시 1초 이내에 E2E 페이지로 전환된다.
- [ ] 세션 이름이 카드 폭을 초과할 경우 말줄임표(`…`)로 잘려 표시되며 줄바꿈되지 않는다.
- [ ] 3개 카드의 미리보기 영역 높이가 정확히 동일하다 (150px).

##### 알려진 제약

- ❌ 모든 세션 카드 클릭이 같은 E2E 페이지로 이동 — 클릭한 세션의 컨텍스트(이름/블록 상태 등)가 E2E 페이지에 반영되지 않음.
- ❌ `View all` 버튼은 동작하지 않음 — 전체 세션 리스트 페이지/모달 미구현.
- ❌ 카드 데이터 3개 모두 하드코딩 — 실제 사용자의 최근 작업 이력 미연동.
- ❌ 썸네일이 placeholder 패턴(스트라이프) — 실제 이미지 표시 안 됨.
- ❌ `active` 배지가 모든 카드에 동일하게 표시 — 실제 활성 여부와 무관.

##### 평가 기준 없음

- 카드 개수가 3개 이상일 경우의 페이징/캐러셀 동작 기준 없음.
- 시간 표시(`2 min ago` / `Yesterday`) 포맷의 자동 갱신 정책 기준 없음.

---

#### FR-HOME-05 — 활동 로그 (Activity)

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (정적 5건, 클릭 동작 없음) |
| **구현 위치** | `/whale-app/src/components/Home.tsx` |

##### 행위

**조건**: 사용자가 Home 페이지의 가장 아래 영역에 도달한다.

**액션 1 — 섹션 진입**:
- 섹션 헤더에 `Activity` 제목과 우측에 `All logs >` 버튼이 표시된다.
- 그 아래 흰색 카드 안에 5개의 활동 항목이 위에서 아래로 나열된다.

**액션 2 — 활동 항목 표시**:
각 항목은 4-column 그리드로 다음 정보를 표시한다:

| 아이콘 | 메시지 | 작성자 | 시간 |
|--------|--------|--------|------|
| pipeline | Updated **Tone Curve** on **HDR · outdoor portrait** | jiwon.k | 12:44 |
| check | Approved simulation set **night-A-rev7** (18 images) | j.park | 11:02 |
| upload | Uploaded 6 references to **ColorChecker SG** | jiwon.k | 09:18 |
| tag | Added tag `low-light` to 14 images | s.nam | Yesterday |
| folder | Created collection **Sensor-A · Night Tuning Pass 3** | jiwon.k | Apr 16 |

- 아이콘은 좌측, 메시지는 가운데(가변폭), 작성자와 시간은 우측 끝에 모노스페이스로 표시된다.
- 메시지 내 강조 부분은 굵게(`<b>`), 태그는 액센트 색 배지로 표시된다.
- 항목 간 1px 구분선이 그어진다.

**액션 3 — 항목 클릭**:
- 시각적 피드백(hover 등)이 있으나 페이지 전환이나 상세 보기 동작이 발생하지 않는다.

**액션 4 — All logs 버튼 클릭**:
- 시각적 피드백만 있고 페이지 전환 등의 동작이 발생하지 않는다.

##### 측정 기준

- [ ] 각 항목의 4개 컬럼(아이콘/메시지/작성자/시간)이 카드 폭에 맞춰 정렬된다.
- [ ] 작성자와 시간은 모노스페이스 폰트로 시각적 정렬이 깨지지 않는다.
- [ ] 항목 간 구분선이 마지막 항목 이후에는 표시되지 않는다.

##### 알려진 제약

- ❌ 5개 항목 모두 하드코딩 — 실제 활동 이벤트 스트림 연동 없음.
- ❌ `All logs` 버튼이 동작하지 않음 — 전체 로그 페이지/모달 미구현.
- ❌ 활동 항목 클릭 시 해당 액션의 컨텍스트로 이동하는 동작 없음 (예: "Updated Tone Curve" 클릭 → 해당 세션의 E2E 톤 커브 패널로 이동).
- ❌ 시간 표시(`12:44` / `Yesterday` / `Apr 16`)가 자동 갱신되지 않음.
- ❌ 사용자별/유형별 필터 미구현.

##### 평가 기준 없음

- 항목 개수가 많아질 때의 페이징, 무한 스크롤, "더 보기" 등의 동작 기준 없음.
- 다른 사용자(`j.park`, `s.nam`)와 본인(`jiwon.k`)의 활동을 시각적으로 구분해야 하는지 기준 없음.

---

### 1.2 Library 페이지

> **개요**: 사용자가 그동안 작업한 reference / simulation 이미지를 한곳에서 관리하는 페이지. 좌측 사이드바로 컬렉션 · 세션 · 태그를 필터링하고, 우측 메인 영역에서 Grid 또는 List 뷰로 항목을 보고 다중 선택해 일괄 액션(태그 추가, 즐겨찾기, E2E로 보내기 등)을 수행한다.
>
> **구현 위치**: `/whale-app/src/components/Library.tsx` · `/whale-app/src/data.ts`

---

#### FR-LIB-01 — 페이지 레이아웃 (사이드바 + 메인 영역)

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/components/Library.tsx` |

##### 행위

**조건**: 사용자가 상단바에서 [Library] 탭을 클릭한다.

**액션**: Library 페이지가 로드된다.

**결과**:
1. 화면이 좌측 220px 폭의 **사이드바**와 우측 가변 폭의 **메인 영역**으로 2분할된다.
2. 사이드바는 위에서부터 `Collections` / `Sessions` / `Tags` 세 영역으로 구성된다.
3. 메인 영역은 위에서부터 다음 4단으로 구성된다:
   - **상단바 (`lib-bar`)**: 페이지 제목, 검색 입력, 소스 필터, Filter 버튼, 뷰 전환, Upload 버튼
   - **서브바 (`lib-sub`)**: 표시 개수 / 선택 개수 / 일괄 액션 / 정렬·그룹 표시
   - **콘텐츠 영역 (`lib-content`)**: Grid 또는 List 형태의 이미지 목록
4. 메인 영역의 폭이 좁아져도 콘텐츠가 사이드바를 밀어내지 않고 컨테이너 안에서 줄어든다.
5. 사이드바와 메인 영역 사이에 1px 구분선이 표시된다.

##### 측정 기준

- [ ] 사이드바 폭이 정확히 220px이며 윈도우 리사이즈 시에도 유지된다.
- [ ] 메인 영역의 폭이 줄어들 때 가로 스크롤이 발생하지 않고 콘텐츠가 자연스럽게 줄어든다.
- [ ] 사이드바와 메인 영역 사이에 1px 구분선이 표시된다.

##### 알려진 제약

- ❌ 사이드바 폭은 고정값(220px)이며 사용자가 드래그로 조절할 수 없다.
- ❌ 사이드바 접기/펼치기 토글이 없다.

##### 평가 기준 없음

- 모바일/태블릿 등 좁은 화면에서의 반응형 동작 기준 없음 (현재는 데스크탑 전용).

---

#### FR-LIB-02 — Collections 사이드바 필터

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (필터 동작은 일부만, 카운트 정합성 없음) |
| **구현 위치** | `/whale-app/src/components/Library.tsx` · `/whale-app/src/data.ts` |

##### 행위

**조건**: 사용자가 Library 페이지 사이드바의 Collections 섹션을 본다.

**액션 1 — 섹션 진입**:
- 섹션 헤더에 `COLLECTIONS` 라벨과 우측에 `+` 버튼이 표시된다.
- 그 아래 컬렉션 항목 4개가 위에서 아래로 나열된다: `All images` (1284) / `References` (420) / `Simulations` (864) / `Favorites` (42).
- 각 항목은 좌측 아이콘 + 컬렉션 이름 + 우측에 모노스페이스 카운트로 구성된다.

**액션 2 — 컬렉션 항목 클릭**:
1. 클릭한 항목이 액센트 색 배경(`--accent-soft`)으로 활성 표시되고, 다른 항목은 비활성으로 돌아간다.
2. 메인 영역 상단바의 페이지 제목(`<h2>`)이 선택된 컬렉션 이름으로 바뀐다 (선택 항목이 없을 때는 `All images`).
3. 메인 콘텐츠가 해당 컬렉션 조건으로 필터링된다:
   - `Favorites` (id: `fav`) → `fav: true`인 항목만 표시
   - `References` (id: `ref`) → `src === 'ref'`인 항목만 표시
   - `Simulations` (id: `sim`) → `src === 'sim'`인 항목만 표시
   - `All images` (id: `all`) → 전체 표시 (별도 매칭 조건 없음)

**액션 3 — `+` 버튼 클릭**:
- 시각적 피드백(hover)만 있고, 새 컬렉션을 추가하는 다이얼로그가 나타나지 않는다.

##### 측정 기준

- [ ] 어느 시점에서도 활성 표시된 항목이 정확히 1개이다.
- [ ] 컬렉션 클릭 후 1초 이내에 메인 콘텐츠와 페이지 제목이 갱신된다.
- [ ] `Favorites` 클릭 시 `fav: true`인 항목만 표시된다.
- [ ] `References` / `Simulations` 클릭 시 해당 `src` 값을 가진 항목만 표시된다.

##### 알려진 제약

- ❌ `+` 버튼은 동작하지 않음 — 새 컬렉션 추가 흐름 미구현.
- ❌ 사이드바에 표시된 컬렉션 카운트(`1284` / `420` / `864` / `42`)는 `data.ts`의 정적 값으로, 실제 데이터(`LIB_ITEMS` 18개)의 필터 결과 개수와 일치하지 않는다.
- ❌ 사용자 정의 컬렉션 생성/편집/삭제 기능 미구현.
- ❌ 페이지 진입 시 기본 활성 컬렉션은 `'all'`이지만 사이드바에는 활성 표시가 안 됨 (페이지 제목만 `All images`로 표시).

##### 평가 기준 없음

- 컬렉션 항목 개수가 사이드바 높이를 초과할 때의 그룹화/축소 정책 기준 없음.

---

#### FR-LIB-03 — Sessions 사이드바 표시

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (표시만 됨, 클릭 핸들러 없음) |
| **구현 위치** | `/whale-app/src/components/Library.tsx` · `/whale-app/src/data.ts` |

##### 행위

**조건**: 사용자가 Library 페이지 사이드바의 Sessions 섹션을 본다.

**액션 1 — 섹션 진입**:
- 섹션 헤더에 `SESSIONS` 라벨과 우측에 `+` 버튼이 표시된다.
- 그 아래 세션 6개가 나열된다: `night-A · Pass 3` (18) / `hdr-outdoor` (24) / `calibration · D65` (36) / `skin-study` (12) / `auto-wb` (48) / `golden-hour` (22).
- 각 항목은 폴더 아이콘 + 세션 이름(긴 경우 ellipsis) + 카운트로 구성된다.
- `night-A · Pass 3`은 `active: true`이므로 액센트 색 배경으로 강조 표시된다.

**액션 2 — 세션 항목 클릭**:
- 시각적 피드백(hover)만 있고, 메인 콘텐츠 필터링이나 활성 표시 변경이 발생하지 않는다.

**액션 3 — `+` 버튼 클릭**:
- 시각적 피드백만 있고 동작 없음.

##### 측정 기준

- [ ] 세션 이름이 사이드바 폭을 초과할 경우 말줄임표(`…`)로 잘려 한 줄로 표시된다.
- [ ] `active: true`인 세션은 액센트 배경으로 일반 세션과 시각적으로 구분된다.

##### 알려진 제약

- ❌ 세션 항목 클릭 시 해당 세션으로 메인 콘텐츠가 필터링되지 않는다 — `onClick` 핸들러 미구현.
- ❌ `active` 표시는 데이터에서 미리 지정된 정적 값이며, 사용자가 클릭해서 활성 세션을 변경할 수 없다.
- ❌ `+` 버튼은 동작하지 않음 — 새 세션 생성 흐름 미구현.

##### 평가 기준 없음

- 세션 항목이 많아질 때의 스크롤/페이징 정책 기준 없음.

---

#### FR-LIB-04 — Tags 다중 선택 필터

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (선택 토글만 동작, 메인 콘텐츠 필터링 미반영) |
| **구현 위치** | `/whale-app/src/components/Library.tsx` |

##### 행위

**조건**: 사용자가 Library 페이지 사이드바의 Tags 섹션을 본다.

**액션 1 — 섹션 진입**:
- 섹션 헤더에 `TAGS` 라벨이 표시된다 (`+` 버튼 없음).
- 그 아래 태그 칩(`lib-tag`)들이 가로로 배치되며, 한 줄을 넘으면 다음 줄로 줄바꿈된다.
- 각 칩은 태그 이름 + 작은 카운트 숫자로 구성된다 (모노스페이스).
- `ALL_TAGS` 데이터의 처음 12개 태그만 표시된다 (`night` ~ `outdoor`).
- 진입 시 `night` 태그가 기본 활성 상태로 표시된다.

**액션 2 — 태그 칩 클릭**:
1. 클릭한 칩이 활성/비활성 상태로 토글된다.
2. 활성 상태: 검은 배경(`--fg`) + 흰 텍스트.
3. 비활성 상태: 회색 배경(`--bg-subtle`) + 일반 보더.
4. 여러 태그를 동시에 활성화할 수 있다 (다중 선택).

**액션 3 — 태그 칩 hover**:
- 텍스트 색이 진해지고 보더 색이 강조된다.

##### 측정 기준

- [ ] 활성 태그와 비활성 태그가 색상으로 명확히 구분된다.
- [ ] 여러 태그를 동시에 활성화할 수 있다.
- [ ] 같은 태그를 다시 클릭하면 비활성 상태로 돌아간다.
- [ ] 사이드바에 정확히 12개의 태그만 표시된다.

##### 알려진 제약

- ❌ 태그 활성화 상태가 메인 콘텐츠 필터링에 반영되지 않는다 — `activeTags` state는 변경되지만 `filtered` 계산에 사용되지 않음.
- ❌ 13번째 이후의 태그(`backlit`, `foliage`, `sunset`, `rev-7`, `rev-5`)는 사이드바에 표시되지 않으며, "더 보기" 또는 검색 기능이 없다.
- ❌ 태그를 직접 추가/편집/삭제하는 기능이 없다.

##### 평가 기준 없음

- 태그 다중 선택 시 AND/OR 결합 방식의 디자인 의도 기준 없음 (현재는 적용 자체가 안 되므로 결정 보류 상태).

---

#### FR-LIB-05 — 검색 및 도구 모음 (상단바)

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (Source 필터·View 전환만 동작, 검색·Filter·Upload는 UI만) |
| **구현 위치** | `/whale-app/src/components/Library.tsx` |

##### 행위

**조건**: 사용자가 Library 페이지 메인 영역의 상단바를 본다.

**액션 1 — 상단바 구성 확인**:
좌측부터 다음 순서로 컴포넌트가 배치된다:
1. 페이지 제목 (`<h2>`, 16px) — 현재 활성 컬렉션 이름
2. 검색 입력 (`Search images, tags, sessions…` placeholder, 좌측에 돋보기 아이콘, 폭 260px)
3. 가변 spacer (좌측 그룹과 우측 그룹을 분리)
4. Source 세그먼트 필터 (`All` / `Ref` / `Sim`)
5. `Filter` 버튼 (필터 아이콘 + 라벨)
6. View 세그먼트 전환 (Grid 아이콘 / Rows 아이콘)
7. `Upload` primary 버튼 (업로드 아이콘 + 라벨)

**액션 2 — Source 세그먼트 클릭** (`All` / `Ref` / `Sim`):
1. 클릭한 옵션이 흰색 배경으로 활성 표시된다.
2. 메인 콘텐츠가 즉시 필터링된다:
   - `All`: 모든 항목 표시
   - `Ref`: `src === 'ref'`인 항목만 표시
   - `Sim`: `src === 'sim'`인 항목만 표시
3. 서브바의 `N of M items` 카운트가 즉시 갱신된다.

**액션 3 — 검색 입력 클릭/타이핑**:
- 입력창이 포커스되고 텍스트를 입력할 수 있지만, 입력값이 메인 콘텐츠 필터링에 반영되지 않는다.

**액션 4 — Filter 버튼 클릭**:
- 시각적 피드백만 있고 필터 패널/모달이 나타나지 않는다.

**액션 5 — Upload 버튼 클릭**:
- 시각적 피드백만 있고 파일 업로드 다이얼로그나 업로드 흐름이 나타나지 않는다.

##### 측정 기준

- [ ] Source 세그먼트 클릭 후 1초 이내에 메인 콘텐츠와 카운트가 갱신된다.
- [ ] 활성 컬렉션 이름이 `<h2>`에 즉시 반영된다.
- [ ] Source 세그먼트는 항상 `All` / `Ref` / `Sim` 중 정확히 1개가 활성 표시된다.

##### 알려진 제약

- ❌ 검색 입력은 placeholder만 있고 실제 텍스트 필터링이 동작하지 않는다.
- ❌ 검색 입력은 `value` / `onChange` 바인딩이 없는 uncontrolled input이며 상태 관리가 없다.
- ❌ Filter 버튼은 동작하지 않음 — 추가 필터 옵션 패널 미구현.
- ❌ Upload 버튼은 동작하지 않음 — 파일 업로드 다이얼로그 / 드래그앤드롭 미구현.
- ❌ Source 필터(`srcFilter`)와 Collections 필터(`filter`)가 독립적으로 작동하므로, `References` 컬렉션 + Source `Sim` 조합 시 결과가 0개로 비어보일 수 있다.

##### 평가 기준 없음

- 검색 결과의 매칭 범위(이름만 vs 이름+태그+세션 등) 기준 없음 (placeholder 문구는 모두 검색 가능한 것처럼 시사하나 미구현).

---

#### FR-LIB-06 — Grid / List 뷰 전환

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/components/Library.tsx` |

##### 행위

**조건**: 사용자가 Library 페이지에서 메인 콘텐츠를 본다. 진입 시 기본 뷰는 Grid이다.

**액션 1 — Grid 뷰 표시**:
- 컨테이너 폭에 맞춰 카드들이 자동으로 여러 열로 배치된다 (각 카드 최소 190px, 남는 공간 균등 분배).
- 각 카드는 다음 구조로 표시된다:
  - 상단: 4:3 비율 썸네일 (placeholder 패턴, 좌상단에 `REF` / `SIM` 배지)
  - 우상단: 즐겨찾기 별 버튼 (반투명 흰색 배경)
  - 하단 메타: 파일명(모노스페이스, 한 줄, ellipsis) + 크기·날짜
- 컨테이너 폭이 변하면 자동으로 열 개수가 늘거나 줄어든다.

**액션 2 — View 세그먼트의 List(rows) 아이콘 클릭**:
1. 컨텐츠가 즉시 테이블 뷰로 전환된다.
2. 테이블 헤더(`Name` / `Source` / `Tags` / `Session` / `Dimensions` / `Date`)가 표시된다.
3. 각 행은 체크박스, 작은 썸네일(48×32), 파일명(즐겨찾기 별 포함), 소스 배지, 태그(최대 3개), 세션, 크기, 날짜, 더보기 버튼으로 구성된다.
4. 헤더와 행이 동일한 컬럼 폭(`24px 56px 2fr 0.8fr 1.4fr 0.9fr 0.9fr 0.7fr 0.5fr`)으로 정렬된다.

**액션 3 — View 세그먼트의 Grid 아이콘 클릭**:
- 다시 Grid 뷰로 즉시 전환된다.

**공통**:
- 두 뷰 모두 같은 `filtered` 데이터를 사용하므로 필터 결과가 동일하다.
- 뷰 전환 시 선택 상태(`selected`)가 유지된다.

##### 측정 기준

- [ ] Grid 뷰에서 컨테이너 폭이 변할 때 카드가 잘리거나 가로 스크롤이 생기지 않는다.
- [ ] List 뷰에서 헤더와 모든 행의 컬럼 폭이 정확히 일치한다.
- [ ] 뷰 전환 후 선택된 항목 표시(체크박스 / 액센트 보더)가 그대로 유지된다.
- [ ] 뷰 전환은 1초 이내에 완료된다.

##### 알려진 제약

- ❌ 사용자가 마지막에 선택한 뷰 모드가 페이지 재방문 시 유지되지 않음 (localStorage 등 영구 저장 미구현).
- ❌ List 뷰의 컬럼 폭은 고정 비율이며 사용자가 드래그로 조절할 수 없다.
- ❌ List 뷰에서 컬럼 헤더 클릭으로 정렬 기능이 동작하지 않음.
- ❌ 행/카드의 더보기(`···`) 버튼 클릭 시 컨텍스트 메뉴가 나타나지 않음.
- ❌ List 뷰는 `<table>` 대신 div + grid로 구현되어 있어 스크린리더 호환성이 떨어진다 (시멘틱 손실).

##### 평가 기준 없음

- 카드 최소 폭(현재 190px) 결정의 디자인 기준 명시 없음.
- 서브바에 표시된 `sort: date ↓` / `· group: session` 텍스트가 실제 정렬/그룹 동작을 의미하는지에 대한 기준 없음 (현재는 정적 라벨).

---

#### FR-LIB-07 — 다중 선택 및 일괄 액션

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (선택만 동작, 일괄 액션 4종 모두 mock) |
| **구현 위치** | `/whale-app/src/components/Library.tsx` |

##### 행위

**조건**: 사용자가 Library 페이지 메인 콘텐츠를 본다. 진입 시 ID 1, 2 항목이 기본 선택 상태이다.

**액션 1 — Grid 뷰 카드 클릭 / List 뷰 행 클릭**:
1. 클릭한 항목의 선택 상태가 토글된다 (선택 ↔ 해제).
2. Grid 뷰: 카드 보더가 액센트 색으로 변하고, 외곽에 글로우(`box-shadow`)가 표시되며, 좌상단에 액센트 색 체크박스가 나타난다.
3. List 뷰: 행 배경이 `--accent-soft`로 변하고, 좌측 체크박스가 액센트 색으로 채워진다.
4. 같은 항목을 다시 클릭하면 선택 해제된다.

**액션 2 — Grid 뷰 카드 hover**:
- 비선택 카드는 hover 시 좌상단에 빈 체크박스가 미리 보여진다 (선택 가능함을 시각적으로 시사).

**액션 3 — 선택 개수 표시 (서브바)**:
- 서브바 좌측에 `{filtered.length} of {LIB_ITEMS.length} items · {selected.size} selected` 형식으로 모노스페이스 표기된다.

**액션 4 — 선택된 항목이 1개 이상일 때**:
서브바에 다음 4개 액션 버튼이 동적으로 등장한다:
- `Tag` (태그 아이콘 + 라벨)
- `Add to…` (폴더 아이콘 + 라벨)
- `Favorite` (별 아이콘 + 라벨)
- `Compare in E2E` (split 아이콘 + 라벨, primary 스타일)

선택이 0개로 돌아가면 4개 버튼이 사라진다.

**액션 5 — 액션 버튼 클릭**:
- 시각적 피드백(hover, 클릭 누름)만 있고, 실제 동작이 발생하지 않는다.

##### 측정 기준

- [ ] 항목 클릭 후 즉시(100ms 이내) 선택 표시가 시각적으로 반영된다.
- [ ] 선택 개수가 서브바에 정확하게 표시된다.
- [ ] 일괄 액션 버튼은 선택 0개일 때 사라지고, 1개 이상일 때 등장한다.
- [ ] 뷰 전환(Grid ↔ List) 후에도 선택 상태가 유지된다.

##### 알려진 제약

- ❌ Shift + 클릭으로 범위 선택 미지원.
- ❌ 전체 선택 / 전체 해제 토글 버튼 없음.
- ❌ `Tag`, `Add to…`, `Favorite`, `Compare in E2E` 버튼 4종 모두 동작하지 않음 — 모달/액션 흐름 미구현.
- ❌ 페이지 진입 시 ID 1, 2가 기본 선택된 것은 데모 목적 하드코딩이며, 실제 사용 시나리오에서는 비어있어야 자연스럽다.
- ❌ ESC 키로 선택 해제 미지원.

##### 평가 기준 없음

- 일괄 액션 버튼이 등장/사라질 때의 애니메이션 기준 없음 (현재는 즉시 표시/숨김).

---

#### FR-LIB-08 — 즐겨찾기 및 메타데이터 표시

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (즐겨찾기 시각 표시만, 토글 기능 미구현) |
| **구현 위치** | `/whale-app/src/components/Library.tsx` |

##### 행위

**조건**: 사용자가 Library 페이지에서 이미지 항목을 본다.

**액션 1 — 즐겨찾기 시각 표시**:
- Grid 뷰: 카드 우상단에 별 아이콘 버튼이 항상 표시된다.
  - `fav: true` 항목 → 노란색 채워진 별 (`oklch(0.75 0.15 75)`)
  - `fav: false` 항목 → 회색 빈 별 (`--fg-subtle`)
- List 뷰: 파일명 좌측에 `fav: true`인 경우만 작은 노란 별 아이콘이 인라인으로 표시된다.

**액션 2 — Grid 뷰 별 버튼 클릭**:
- 클릭 이벤트가 카드 클릭으로 전파되지 않는다 (`stopPropagation` 적용 → 선택 토글 안 됨).
- 시각적 피드백(hover, 클릭 누름)만 있고, 즐겨찾기 상태 토글이 발생하지 않는다.

**액션 3 — Source 배지 표시**:
- Grid 뷰: 썸네일 좌상단에 `REF` 또는 `SIM` 배지 (소문자 → 대문자 변환).
  - `ref` 항목은 일반 배지 스타일, `sim` 항목은 `sim` 보조 스타일 적용.
- List 뷰: Source 컬럼에 동일한 배지 표시.

**액션 4 — 메타데이터 표시**:
- Grid 뷰 카드 하단:
  - 파일명 (예: `night_0042_ref.dng`) — 모노스페이스, 11px, 한 줄 ellipsis
  - 크기 (예: `6000×4000`) 좌측 + 날짜 (예: `Apr 18`) 우측 — 모노스페이스, 10px
- List 뷰 행:
  - 파일명, 소스 배지, 태그(최대 3개 칩), 세션, 크기, 날짜 — 각 컬럼별 정렬

##### 측정 기준

- [ ] `fav: true` 항목과 `fav: false` 항목의 별 색상이 명확히 구분된다.
- [ ] 별 버튼 클릭 시 카드 선택 상태가 변경되지 않는다 (이벤트 전파 차단 확인).
- [ ] Grid 뷰 파일명이 카드 폭을 초과할 경우 말줄임표(`…`)로 잘려 한 줄로 표시된다.
- [ ] List 뷰 태그 칩이 컬럼 폭을 초과하지 않고 잘려서 표시된다 (overflow hidden).

##### 알려진 제약

- ❌ 별 버튼 클릭 시 `fav` 상태가 토글되지 않음 — 데이터를 state로 끌어올리는 로직 미구현.
- ❌ 즐겨찾기 변경 사항이 영구 저장되지 않음 (페이지 새로고침 시 초기 상태로 복귀).
- ❌ List 뷰에서 태그가 4개 이상인 항목은 처음 3개만 표시되며 "+N more" 같은 인디케이터가 없다.
- ❌ 우측 더보기(`···`) 버튼은 시각적 표시만 있고 동작 없음.
- ❌ List 뷰 썸네일 클릭 시 행 클릭과 동일한 선택 토글이 발생한다 (확대 미리보기 등 별도 동작 없음).

##### 평가 기준 없음

- 즐겨찾기 별 색상(`oklch(0.75 0.15 75)`) 선택의 디자인 기준 명시 없음.
- List 뷰의 인라인 별 아이콘과 Grid 뷰의 별 버튼이 서로 다른 인터랙션을 가져야 하는지에 대한 기준 없음.

---

### 1.3 E2E 페이지

> **개요**: 사용자가 실제 튜닝 작업을 수행하는 핵심 페이지. 좌측 메인 영역에서 reference / simulation 이미지를 비교 보기(Compare Viewer)하고, 그 아래에서 처리 파이프라인(Pipeline)을 Node / List / Timeline 세 가지 뷰로 살펴보며, 우측 패널(Tuning Panel)에서 선택된 블록의 파라미터를 실시간으로 조정한다.
>
> **구현 위치**: `/whale-app/src/components/e2e/E2EPage.tsx` · `/whale-app/src/components/e2e/CompareViewer.tsx` · `/whale-app/src/components/e2e/NodeGraph.tsx` · `/whale-app/src/components/e2e/StepList.tsx` · `/whale-app/src/components/e2e/Timeline.tsx` · `/whale-app/src/components/e2e/TuningPanel.tsx`

---

#### FR-E2E-01 — 페이지 레이아웃 및 Pipeline 영역 세로 리사이즈

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/components/e2e/E2EPage.tsx` |

##### 행위

**조건**: 사용자가 상단바에서 [E2E] 탭을 클릭한다.

**액션 1 — 진입 시 레이아웃 표시**:
1. 화면이 좌우 2분할로 표시된다:
   - **좌측 메인 영역**: 가변 폭, 위에서 아래로 `Compare Viewer` (가변 높이) → `세로 리사이저` (6px) → `Pipeline 영역` (기본 260px)으로 3단 구성
   - **우측 Tuning Panel**: 320px 고정 폭
2. 진입 시 기본 선택 상태:
   - 비교 모드(`cmpMode`): `split`
   - 파이프라인 뷰(`pipeView`): `node`
   - 선택된 블록(`sel`): `tone`
   - Bypass 상태: `false` (active)

**액션 2 — Pipeline 영역 리사이저(`e2e-resizer`) 드래그**:
1. 리사이저에 마우스를 올리면 커서가 row-resize로 변하고, 가운데 28×2px 회색 그립 표시가 보인다.
2. 마우스 누름 후 위/아래로 드래그하면 Pipeline 영역의 높이가 변한다 (위로 드래그 → Pipeline 커짐, 아래로 드래그 → 작아짐).
3. 드래그 중에는 Compare Viewer 영역의 높이가 자동으로 조정된다.
4. 마우스 버튼을 떼면 드래그가 종료되고 새 높이가 유지된다.
5. 컨테이너 밖으로 마우스가 나가도 `window` 리스너로 추적되어 드래그가 끊기지 않는다.

**액션 3 — Pipeline 높이 클램프**:
- 리사이저 드래그로 설정 가능한 높이는 최소 140px, 최대 500px이다.

##### 측정 기준

- [ ] Tuning Panel 폭이 정확히 320px이며 윈도우 리사이즈 시에도 유지된다.
- [ ] Pipeline 영역 높이는 140px ~ 500px 범위로 클램프된다.
- [ ] 리사이저 드래그 중 컨테이너 밖으로 마우스가 나가도 추적이 끊기지 않는다.
- [ ] 진입 시 Compare(split) / Pipeline(node) / Tuning(tone) 기본값이 모두 활성 표시된다.

##### 알려진 제약

- ❌ Tuning Panel과 메인 영역 사이의 좌우 리사이즈 미지원 (Tuning Panel 폭 고정).
- ❌ 리사이저 위치 / Pipeline 높이가 페이지 재방문 시 유지되지 않음 (localStorage 미적용).
- ❌ 키보드(예: 화살표 키)로 리사이저 조작 미지원.

##### 평가 기준 없음

- 리사이저 드래그 중 미리보기(half-tone 가이드 등) 추가 여부에 대한 디자인 기준 없음.

---

#### FR-E2E-02 — 비교 뷰어 — 3종 비교 모드 (Split / Slider / Overlay)

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/components/e2e/CompareViewer.tsx` |

##### 행위

**조건**: 사용자가 Compare Viewer 영역의 툴바를 본다.

**액션 1 — 모드 세그먼트 진입 표시**:
- 툴바 좌측에 3개 세그먼트(`Split` / `Slider` / `A / B`)가 표시되며 진입 시 `Split`이 활성 상태이다.
- 각 세그먼트에는 라벨과 함께 아이콘(split / slider / layers)이 표시된다.

**액션 2 — Split 모드**:
1. 비교 영역이 좌우 1:1 그리드로 분할된다 (gap 1px, 사이에 1px 보더 효과).
2. 좌측 패널 좌상단에 `A · Reference` 라벨, 좌하단에 `night_ref_042.dng · 14-bit · as shot` 정보 텍스트가 표시된다.
3. 우측 패널 좌상단에 `B · Simulation` 라벨(액센트 색 배경), 좌하단에 `rev-7 · WB 5200K · tone+14 · LUT night-v3` 정보 텍스트가 표시된다.
4. 각 패널은 placeholder 이미지(stripe 패턴)로 채워진다 (좌 hue=210, 우 hue=35).

**액션 3 — Slider 모드 진입**:
1. 좌상단에 `A · Reference`, 우상단에 `B · Simulation` 라벨이 동시에 표시된다.
2. A는 전체 영역을 채우고, B는 가운데 핸들 우측 영역만 보이도록 `clip-path: polygon`으로 잘려 표시된다.
3. 가운데에 흰색 2px 핸들과 원형 그립이 표시된다.

**액션 4 — Slider 핸들 드래그**:
1. 핸들 위 또는 비교 영역의 임의 지점을 클릭/드래그하면 핸들이 그 위치로 이동한다.
2. 핸들 위치는 0~100% 범위로 클램프된다.
3. 핸들 우측 영역의 B 이미지 표시 범위가 핸들 이동에 따라 즉시 갱신된다.
4. 마우스가 영역 밖으로 나가면(`onMouseLeave`) 드래그가 종료된다.

**액션 5 — Overlay (`A / B`) 모드**:
1. 좌상단에 `A/B · BLEND 50%` 라벨이 표시된다.
2. 두 이미지가 동일한 위치에 겹쳐 표시되며, B가 `opacity: 0.5` + `mix-blend-mode: difference`로 합성된다.
3. 두 이미지의 차이가 큰 픽셀일수록 밝게, 동일한 픽셀일수록 어둡게 보인다.

##### 측정 기준

- [ ] 3개 모드 중 정확히 1개가 활성 표시된다.
- [ ] Split 모드에서 좌우 패널 폭이 정확히 1:1이다.
- [ ] Slider 핸들 드래그 시 100ms 이내에 위치가 갱신되며 끊김이 없다.
- [ ] Slider 핸들이 0% 또는 100%를 벗어나지 않는다 (클램프).
- [ ] Overlay 모드에서 두 이미지의 위치가 정확히 겹친다.

##### 알려진 제약

- ❌ Slider 모드에서 마우스가 컨테이너 밖으로 나가면 드래그가 즉시 끊긴다 (window 리스너 패턴 미적용).
- ❌ 터치 디바이스에서 Slider 핸들 드래그 미지원 (`touchstart`/`touchmove`/`touchend` 핸들러 없음).
- ❌ 키보드 화살표 키로 Slider 핸들 조작 미지원.
- ❌ Overlay 블렌드 비율(50%)과 모드(`difference`)는 고정값이며 사용자가 변경할 수 없다.
- ❌ 가로(좌우) 슬라이더만 지원, 세로(상하) 슬라이더 모드 없음.

##### 평가 기준 없음

- Slider 핸들 그립의 시각 표현 (현재 ◂▸ 텍스트) 디자인 기준 명시 없음.
- 모드 전환 시 트랜지션 애니메이션 적용 여부 기준 없음.

---

#### FR-E2E-03 — 비교 뷰어 — 줌 컨트롤 및 도구 모음

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (줌 표시값만 변경, 실제 이미지 확대 안 됨; Fit/Mask/Picker/Commit는 UI만) |
| **구현 위치** | `/whale-app/src/components/e2e/CompareViewer.tsx` |

##### 행위

**조건**: 사용자가 Compare Viewer 영역 상단의 툴바를 본다.

**액션 1 — 툴바 구성 확인**:
좌측부터 다음 순서로 표시된다:
1. 모드 세그먼트 (Split / Slider / A·B) — FR-E2E-02 참조
2. `Fit` 버튼 (reset 아이콘 + 라벨)
3. 줌 컨트롤 (`−` / `100%` / `+` 가로 일체형 그룹)
4. spacer (좌측과 우측 그룹 분리)
5. `1920 × 1080` 배지 (모노스페이스)
6. `sRGB · 8-bit` 배지 (모노스페이스)
7. `Mask` 버튼 (eye 아이콘 + 라벨)
8. `Picker` 버튼 (droplet 아이콘 + 라벨)
9. `Commit` primary 버튼 (check 아이콘 + 라벨)

**액션 2 — 줌 `−` 버튼 클릭**:
- 줌 값이 25 단위로 감소하며, 최소 25%로 클램프된다.
- 가운데 표시값(`100%`)이 즉시 갱신된다.

**액션 3 — 줌 `+` 버튼 클릭**:
- 줌 값이 25 단위로 증가하며, 최대 400%로 클램프된다.
- 가운데 표시값이 즉시 갱신된다.

**액션 4 — Fit / Mask / Picker / Commit 버튼 클릭**:
- 시각적 피드백(hover, 클릭 누름)만 있고, 실제 동작이 발생하지 않는다.

##### 측정 기준

- [ ] 줌 값이 25% ~ 400% 범위 내로 클램프된다.
- [ ] 줌 표시값이 정수 + `%` 포맷으로 표시된다.
- [ ] 줌 컨트롤 가운데 영역의 최소 폭(54px)이 유지되어 자릿수 변동에도 좌/우 버튼 위치가 변하지 않는다.

##### 알려진 제약

- ❌ 줌 변경이 실제 이미지 표시 크기에 적용되지 않음 — placeholder 이미지(`PhImg`)에 `transform: scale` 등이 연결되어 있지 않다.
- ❌ `Fit` 버튼은 동작하지 않음 — 컨테이너에 맞춰 자동 fit 로직 미구현.
- ❌ `Mask` 버튼은 동작하지 않음 — 마스크 표시 토글 미구현.
- ❌ `Picker` 버튼은 동작하지 않음 — 픽셀 픽킹 모드 진입 미구현.
- ❌ `Commit` 버튼은 동작하지 않음 — 변경 사항 저장 흐름 미구현.
- ❌ `1920 × 1080`, `sRGB · 8-bit` 배지는 정적 텍스트 — 실제 이미지 메타데이터와 무관.
- ❌ 마우스 휠 / 트랙패드 핀치로 줌 조작 미지원.

##### 평가 기준 없음

- 줌 단위(25%) 결정의 디자인 기준 없음.
- 색공간/비트 깊이 표시(`sRGB · 8-bit`)의 표시 정책 기준 없음.

---

#### FR-E2E-04 — 비교 뷰어 — 메트릭 및 픽셀 정보 표시

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (정적 표시, 데이터 연동 없음) |
| **구현 위치** | `/whale-app/src/components/e2e/CompareViewer.tsx` |

##### 행위

**조건**: 사용자가 Compare Viewer 영역의 하단 바를 본다.

**액션 — 하단 바 진입 표시**:
1. 좌측에 5개의 메트릭이 모노스페이스로 가로 배치된다 (각각 라벨 + 값):
   - `ΔE₀₀` : `2.84`
   - `PSNR` : `34.2 dB`
   - `SSIM` : `0.962`
   - `WB ref` : `5180 K / +6`
   - `WB sim` : `5200 K / +4`
2. 라벨은 작은 대문자(`text-transform: uppercase`) + faint 색, 값은 일반 본문 색.
3. 우측에 픽셀 포인터 정보가 모노스페이스 + faint 색으로 표시된다:
   - `pointer: 912, 488 · R 182 G 144 B 108 · L* 62.4`

##### 측정 기준

- [ ] 5개 메트릭과 픽셀 정보가 모노스페이스 폰트로 정렬된다.
- [ ] 좌측 메트릭 그룹과 우측 픽셀 정보가 spacer로 분리되어 영역 폭이 변해도 양 끝에 붙는다.

##### 알려진 제약

- ❌ 5개 메트릭(`ΔE₀₀`, `PSNR`, `SSIM`, `WB ref`, `WB sim`) 모두 하드코딩된 정적 값 — 실제 이미지 비교 계산 결과와 무관.
- ❌ 픽셀 포인터 정보(`pointer: 912, 488 · ...`)도 정적 텍스트 — 실제 마우스 위치 / 픽셀 값 추적 미구현.
- ❌ 비교 모드(Split / Slider / Overlay) 변경 시 메트릭 값이 갱신되지 않음 (어차피 정적 값).
- ❌ 메트릭이 임계값을 초과할 때(예: ΔE > 3.0) 경고 색으로 강조하는 동작 없음.

##### 평가 기준 없음

- 메트릭 5종의 선택 기준 (왜 이 5개인지) 명시 없음.
- 메트릭 클릭 시 상세 / 히스토그램 / 시각화 패널을 여는 동작이 필요한지에 대한 기준 없음.

---

#### FR-E2E-05 — Pipeline 헤더 (정보 표시 / 뷰 전환 / 액션 버튼)

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (뷰 전환만 동작, Add block / Run / Live는 UI만) |
| **구현 위치** | `/whale-app/src/components/e2e/E2EPage.tsx` |

##### 행위

**조건**: 사용자가 E2E 페이지 메인 영역의 Pipeline 섹션 상단(`e2e-pipe-head`)을 본다.

**액션 1 — 헤더 구성 확인**:
좌측부터 다음 순서로 표시된다:
1. 제목 (`<h4>`, 11px 대문자) : `Pipeline · Sensor-A · Night Tuning Pass 3`
2. `Rev 7` 액센트 dot 배지
3. `13 blocks` 모노스페이스 배지 (PIPELINE 데이터 길이)
4. `~31.5 ms` 모노스페이스 배지 (각 블록 처리 시간 합계, 동적 계산)
5. spacer
6. 뷰 전환 세그먼트 (`Node` / `List` / `Timeline`, 각 세그먼트는 아이콘 + 라벨)
7. `Add block` 버튼 (plus 아이콘 + 라벨)
8. `Run` 버튼 (play 아이콘 + 라벨)
9. `Live` primary 버튼 (zap 아이콘 + 라벨)

**액션 2 — 제목 ellipsis**:
- 컨테이너 폭이 좁아져도 제목 영역은 한 줄로 잘려 ellipsis(`…`)로 표시되며, 우측 도구 영역을 밀어내지 않는다.

**액션 3 — 뷰 세그먼트 클릭** (`Node` / `List` / `Timeline`):
1. 클릭한 옵션이 흰색 배경으로 활성 표시된다.
2. Pipeline 본문(`e2e-pipe-body`)이 즉시 해당 뷰로 전환된다.
3. 진입 시 `Node`가 활성 상태이다.
4. 뷰 전환 시 선택된 블록(`sel`)은 유지되며, 우측 Tuning Panel에는 영향이 없다.

**액션 4 — Add block / Run / Live 버튼 클릭**:
- 시각적 피드백만 있고 실제 동작은 발생하지 않는다.

##### 측정 기준

- [ ] 진입 시 `Node` 뷰가 활성 상태이다.
- [ ] `13 blocks` 카운트가 `PIPELINE` 데이터 길이와 일치한다.
- [ ] `~XX.X ms` 시간 합계가 자동 계산되어 표시된다 (소수점 1자리).
- [ ] 헤더 제목이 컨테이너 폭을 초과해도 ellipsis로 잘리고 우측 도구가 밀려나지 않는다.

##### 알려진 제약

- ❌ `Add block` 버튼은 동작하지 않음 — 새 블록 추가 다이얼로그/검색 미구현.
- ❌ `Run` 버튼은 동작하지 않음 — 단발 처리 실행 흐름 미구현.
- ❌ `Live` 버튼은 동작하지 않음 — 실시간 반영 모드 진입 미구현.
- ❌ `Rev 7` 배지는 정적 표시이며 변경/롤백 인터랙션이 없다.
- ❌ 처리 시간(`1.2 + i * 0.31`)은 인덱스 기반 가짜 계산 결과 — 실제 벤치마크 결과 아님.

##### 평가 기준 없음

- 헤더 액션 버튼(Add block / Run / Live)의 활성/비활성 조건(예: 데이터 미준비 시 비활성) 기준 없음.

---

#### FR-E2E-06 — Pipeline Node Graph 뷰

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (선택만 동작, 노드 이동/연결 편집 없음) |
| **구현 위치** | `/whale-app/src/components/e2e/NodeGraph.tsx` |

##### 행위

**조건**: 사용자가 Pipeline 헤더에서 `Node` 뷰를 선택한 상태이다.

**액션 1 — 노드 그리드 진입 표시**:
1. 본문 영역에 13개의 노드가 5열로 배치되며, 줄이 바뀔 때마다 좌→우/우→좌 지그재그(boustrophedon) 순서로 흐른다.
2. 각 노드는 160 × 80 px 카드 형태로:
   - 상단 3px 카테고리 색 바 (`source` / `correct` / `color` / `detail` / `tone` / `output` / `sink` 별 색상 토큰)
   - 좌측에 블록 이름(굵게)
   - 우측에 인덱스(`01` ~ `13`, 모노스페이스, faint 색)
   - 본문에 처음 2개 파라미터가 `key: value · key: value` 형태로 표시
3. 인접 노드는 베지어 곡선(같은 행) 또는 ㄱ자 직선(행 변경)으로 연결된다.
4. 배경에는 14×14px 도트 그리드가 표시된다.
5. 진입 시 `tone` 노드(7번)가 선택 상태(액센트 보더 + 글로우)로 표시된다.

**액션 2 — 노드 클릭**:
1. 클릭한 노드가 선택 상태로 변경된다 (액센트 보더 + 0 0 0 3px 글로우).
2. 다른 노드는 비선택 상태로 돌아간다.
3. 우측 Tuning Panel이 선택된 블록의 컨트롤 UI로 즉시 전환된다.

**액션 3 — Bypass된 노드 표시**:
- `bypass: true`인 노드(예: `sharp`)는 opacity 0.5로 흐리게 표시된다.

**액션 4 — 노드 hover**:
- 보더 색이 강조 색(`--border-strong`)으로 변한다.

**액션 5 — 입출력 포트 표시**:
- `source` 카테고리 외에는 좌측에 input 포트(원형, 9×9px)가 표시된다.
- `sink` 카테고리 외에는 우측에 output 포트가 표시된다.
- 선택된 노드의 포트는 액센트 색 보더로 변경된다.

**액션 6 — 영역 스크롤**:
- 노드 그리드 폭이 컨테이너를 초과할 경우 가로 스크롤이 가능하다 (overflow: auto).

##### 측정 기준

- [ ] 13개 노드가 5열 지그재그 순서로 배치된다.
- [ ] 인접 노드 간 연결선이 끊기거나 노드를 가리지 않는다.
- [ ] 어느 시점에서도 정확히 1개의 노드만 선택 상태이다.
- [ ] 노드 클릭 후 1초 이내에 Tuning Panel이 갱신된다.

##### 알려진 제약

- ❌ 노드를 드래그해서 위치를 옮길 수 없음 — 레이아웃 알고리즘 결과 그대로 표시.
- ❌ 노드 간 연결선을 사용자가 변경(연결 추가/삭제, 분기) 할 수 없음.
- ❌ 노드 우클릭 시 컨텍스트 메뉴(삭제, 복제 등)가 나타나지 않음.
- ❌ 미니맵, 줌 컨트롤 미구현.
- ❌ 노드 카드의 `bypass`는 데이터 정적 값이며, 카드 자체에서 토글 인터랙션 없음 (Tuning Panel에서만 토글 가능).
- ❌ 노드 본문에는 처음 2개 파라미터만 표시되며, 그 이상은 잘린다 (`...` 표시 없음).

##### 평가 기준 없음

- 노드 카드 폭(160px) / 높이(80px) 결정의 디자인 기준 명시 없음.
- 13개를 초과하는 블록 개수의 레이아웃 정책 (열 개수 자동 조정 등) 기준 없음.

---

#### FR-E2E-07 — Pipeline List 뷰

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (선택만 동작, eye 토글은 UI만) |
| **구현 위치** | `/whale-app/src/components/e2e/StepList.tsx` |

##### 행위

**조건**: 사용자가 Pipeline 헤더에서 `List` 뷰를 선택했다.

**액션 1 — Lane 그룹 진입 표시**:
- 본문 영역에 6개의 카테고리 그룹(Lane)으로 묶여 표시된다:
  - `Source` (cat: `source`)
  - `Correct` (cat: `correct`)
  - `Color` (cat: `color`)
  - `Detail` (cat: `detail`)
  - `Tone` (cat: `tone`)
  - `Output` (cat: `output` 또는 `sink`)
- 각 lane 헤더에 그룹 이름(좌)과 `N blocks` 카운트(우, 모노스페이스 faint)가 표시된다.

**액션 2 — 각 step 행 표시**:
다음 5개 컬럼이 grid로 정렬된다 (28px / 1fr / auto / auto / auto):
1. 인덱스 (`01` ~ `13`, 모노스페이스) + 좌측에 4×22px 카테고리 색 바
2. 블록 이름(굵게) + 그 아래 모든 파라미터를 `key: value · key: value · ...` 모노스페이스로 표시
3. 카테고리 chip (대문자, 모노스페이스, subtle 색)
4. 처리 시간 (`X.X ms`, 모노스페이스, faint)
5. eye 토글 버튼 (eye 또는 eyeOff 아이콘)

**액션 3 — step 행 클릭**:
1. 행 보더가 액센트 색으로 변하고 외곽에 글로우가 표시된다.
2. 우측 Tuning Panel이 선택된 블록의 UI로 갱신된다.
3. 다른 행은 비선택 상태로 돌아간다.

**액션 4 — Bypass된 step 표시**:
- `bypass: true`인 step은 이름과 파라미터에 line-through(취소선) + opacity 0.5로 표시된다.
- 우측 eye 버튼이 `eyeOff` 아이콘으로, opacity 0.4로 흐리게 표시된다.

**액션 5 — eye 버튼 클릭**:
- 시각적 피드백만 있고, bypass 상태가 토글되지 않는다.

##### 측정 기준

- [ ] 어느 시점에서도 정확히 1개의 step이 선택 상태이다.
- [ ] Lane 헤더의 `N blocks` 카운트가 해당 카테고리의 실제 블록 수와 일치한다.
- [ ] Bypass된 step은 line-through로 시각 구분된다.
- [ ] step 행의 5개 컬럼이 grid 폭에 맞춰 정렬된다.

##### 알려진 제약

- ❌ eye 버튼 클릭으로 bypass 토글 안 됨 — `onClick` 핸들러 미구현. (Tuning Panel의 `ACTIVE`/`BYPASSED` 토글로만 변경 가능)
- ❌ step 행 드래그로 순서 변경 / lane 간 이동 미지원.
- ❌ step 행 우클릭 시 컨텍스트 메뉴 미표시.
- ❌ Lane 그룹 접기/펼치기 토글 없음.
- ❌ 검색 / 필터 입력 없음 (블록 수가 많아져도 모두 나열).

##### 평가 기준 없음

- step 행에 표시할 파라미터 개수 제한 정책 기준 없음 (현재는 모두 한 줄로 join, 폭을 초과하면 어떻게 줄어드는지 별도 처리 없음).

---

#### FR-E2E-08 — Pipeline Timeline 뷰

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (선택만 동작, 블록 드래그/리사이즈 없음) |
| **구현 위치** | `/whale-app/src/components/e2e/Timeline.tsx` |

##### 행위

**조건**: 사용자가 Pipeline 헤더에서 `Timeline` 뷰를 선택했다.

**액션 1 — Timeline 진입 표시**:
1. 상단에 가로 시간 스케일이 표시된다 (`0ms` ~ `~12ms`, 2ms 단위 눈금).
2. 그 아래 5개 lane이 위에서 아래로 표시된다:
   - `Correct` (cats: `source`, `correct`)
   - `Color` (cats: `color`)
   - `Detail` (cats: `detail`)
   - `Tone` (cats: `tone`)
   - `Output` (cats: `output`, `sink`)
3. 각 lane은 3-column grid (130px lane 라벨 / 1fr 트랙 / 60px total) 구조이다.
4. lane 트랙 내부에는 해당 lane의 카테고리에 매칭되는 블록만 막대 형태로 표시된다.
5. 각 블록의 좌측 위치는 시간 누적 합 비율로, 폭은 처리 시간 비율로 계산된다.
6. 각 블록은 카테고리 색상의 28% 혼합 배경 + 좌측 3px 카테고리 색 보더로 표시된다.

**액션 2 — 블록 클릭**:
1. 클릭한 블록 외곽에 2px `--fg` 아웃라인이 표시된다 (selected).
2. 우측 Tuning Panel이 갱신된다.
3. 다른 블록은 비선택 상태로 돌아간다.

**액션 3 — 블록 hover**:
- 블록이 세로 방향으로 1.12배 살짝 커진다 (`transform: scaleY(1.12)`).

**액션 4 — Bypass된 블록 표시**:
- `bypass: true`인 블록은 opacity 0.35로 흐리게 표시된다.

**액션 5 — 하단 합계 표시**:
- 영역 하단 우측에 모노스페이스로 `total ~XX.X ms / frame · 1920×1080` 표시.

##### 측정 기준

- [ ] 시간 스케일과 블록 위치가 동일한 비율 기준으로 정렬된다.
- [ ] 어느 시점에서도 정확히 1개의 블록이 선택 상태이다.
- [ ] hover 시 scaleY 효과가 80ms 이내에 적용된다.

##### 알려진 제약

- ❌ 블록을 드래그해서 lane 간 이동 / 순서 변경 미지원.
- ❌ 블록 좌/우 가장자리 드래그로 처리 시간 조정 미지원 (시간은 데이터 정적 값).
- ❌ 시간 스케일을 가로로 줌인/줌아웃 미지원.
- ❌ lane별 처리 시간 합계(`tl-total` 컬럼)가 `—`로 표시되며 실제 합산 미구현.
- ❌ 각 블록의 처리 시간(`1.2 + i * 0.31`)은 인덱스 기반 가짜 계산 — 실제 벤치마크 결과 아님.

##### 평가 기준 없음

- 시간 스케일 단위(현재 2ms) 자동 조정 정책 (총 시간이 매우 짧거나 길 때) 기준 없음.
- 블록이 매우 짧을 때(폭이 1% 미만) 라벨 표시 정책 기준 없음.

---

#### FR-E2E-09 — Tuning Panel — 블록별 분기 컨트롤

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | 🟡 부분 구현 (Tone Curve / Color Wheel 드래그만 실제 state 변경, 그 외 슬라이더 / 셀렉트는 정적) |
| **구현 위치** | `/whale-app/src/components/e2e/TuningPanel.tsx` |

##### 행위

**조건**: 사용자가 좌측 Pipeline 영역에서 임의의 블록을 선택했다. 우측에 Tuning Panel(폭 320px)이 표시된다.

**액션 1 — 패널 헤더 진입 표시**:
1. 헤더 좌측에 다음이 표시된다:
   - 8×8px 카테고리 색 사각형
   - `<h3>` 블록 이름 (예: `Tone Curve`)
   - 그 아래 `block N / 13 · {category}` 모노스페이스 보조 정보
2. 헤더 우측에 `ACTIVE` / `BYPASSED` 토글 버튼:
   - 활성 상태(`!bypass`): `eye` 아이콘 + `ACTIVE` 텍스트, 액센트 색 배경
   - 비활성 상태(`bypass`): `eyeOff` 아이콘 + `BYPASSED` 텍스트, 일반 회색

**액션 2 — Bypass 토글 클릭**:
- 버튼 색상과 텍스트가 즉시 토글된다 (`ACTIVE` ↔ `BYPASSED`).

**액션 3 — Histogram 섹션 (모든 블록 공통)**:
- 본문 최상단에 `Histogram` 섹션이 항상 표시된다.
- 우측에 `RGB + L` 라벨 표시.
- 4채널(R, G, B, Luma)의 정규분포 형태 히스토그램이 SVG path로 겹쳐 표시된다.
- 채널별 색상으로 fill(0.35 alpha) + stroke(0.9 alpha) 그려진다.
- 하단에 0 / 64 / 128 / 192 / 255 눈금이 모노스페이스로 표시된다.

**액션 4 — 블록 타입에 따른 분기 컨트롤**:

| 블록 ID | 컨트롤 |
|---------|--------|
| `tone` (curve) | Tone Curve (드래그 가능 5개 컨트롤 포인트, R/G/B/Luma 채널 전환) + Zones 슬라이더(Shadows / Midtones / Highlights) |
| `hsl` | HSL Adjust 컨트롤 |
| `wheel` | Lift / Gamma / Gain 3개 Color Wheel + Contrast / Saturation / Pivot 슬라이더 |
| `wb` | Temperature(2500~10000K) / Tint 슬라이더 + Pick 버튼 + R/G/B gain 슬라이더 |
| `lut` | LUT 셀렉트 (3개 옵션) + Strength(0~100%) / Gamut bias 슬라이더 + Browse 버튼 |
| `nr`, `sharp` (detail) | 블록의 모든 파라미터를 슬라이더로 자동 생성 |
| 그 외 (generic) | 블록의 모든 파라미터를 텍스트 input으로 자동 생성 |

**액션 5 — Tone Curve 컨트롤 포인트 드래그**:
1. 5개 컨트롤 포인트 중 임의의 점을 드래그하면 점이 따라 이동한다.
2. 양 끝점(0, 4)은 x값이 0 / 255로 잠겨있고, y만 변경 가능.
3. 가운데 3점은 x, y 모두 0~255 범위 내에서 자유 이동.
4. 점 이동 시 곡선(SVG path)이 즉시 갱신된다.
5. window 리스너 패턴으로 영역 밖으로 마우스가 나가도 드래그가 끊기지 않는다.

**액션 6 — Tone Curve 채널 버튼 클릭** (`LUMA` / `R` / `G` / `B`):
- 클릭한 채널의 색상으로 활성 표시된다 (LUMA: 검정, R: 빨강, G: 초록, B: 파랑).
- 그래프의 곡선 색상도 채널 색으로 즉시 변경된다.

**액션 7 — Color Wheel puck 드래그**:
1. 휠 내부의 puck을 드래그하면 puck이 따라 이동한다.
2. puck이 반지름 R(50px)을 벗어나면 같은 방향의 R 위치로 클램프된다.
3. window 리스너 패턴.

**액션 8 — Apply to 섹션** (모든 블록 공통, 본문 하단):
- `Current` / `Scene` / `All` 세그먼트 (진입 시 `Current` 활성).
- 클릭으로 활성 옵션 변경 가능.
- 그 아래 `scope: night-tuning-pass-3 · 18 imgs` 정적 표시.

**액션 9 — 푸터 액션 버튼**:
- `Reset` / `Copy` / `Save preset` (primary, check 아이콘) 버튼 표시.
- 모두 시각적 피드백만 있고 동작하지 않는다.

##### 측정 기준

- [ ] 좌측에서 블록 선택 시 1초 이내에 Tuning Panel UI가 해당 블록 타입에 맞게 갱신된다.
- [ ] Bypass 토글 시 `ACTIVE` / `BYPASSED` 텍스트와 색상이 즉시 변경된다.
- [ ] Histogram 섹션은 모든 블록에서 항상 표시된다.
- [ ] Tone Curve의 양 끝점은 x값이 변경되지 않는다.
- [ ] Color Wheel puck이 원 밖으로 나가지 않는다 (반지름 R로 클램프).

##### 알려진 제약

- ❌ Tone Curve / Color Wheel 외 다른 컨트롤(슬라이더, 셀렉트, 텍스트 input)은 변경값이 별도 state에 저장되지 않거나 mock 콜백(`() => {}`)이며, Compare Viewer / Pipeline 등 다른 영역에 반영되지 않는다.
- ❌ Tone Curve 변경이 실제 이미지 처리에 적용되지 않음 (placeholder 이미지).
- ❌ HSL Adjust의 세부 인터랙션 (어떤 색상을 어떻게 조정하는지) 미문서화.
- ❌ Apply to 세그먼트 변경이 실제 적용 범위에 반영되지 않음.
- ❌ Reset / Copy / Save preset 버튼 모두 동작하지 않음.
- ❌ Bypass 토글이 좌측 Pipeline의 노드/step/timeline의 bypass 표시와 양방향 연동되지 않음 (블록 데이터의 정적 `bypass` 값과 Tuning Panel의 토글이 분리됨).
- ❌ Tone Curve 컨트롤 포인트 추가/삭제 미지원 (5개 고정).
- ❌ 채널별로 Tone Curve points가 분리되지 않음 — 4개 채널이 하나의 points 배열을 공유.
- ❌ Color Wheel의 puck 위치가 hue/saturation 수치로 변환되어 표시되지 않음.

##### 평가 기준 없음

- 블록 타입별 컨트롤 구성의 디자인 의도 (왜 이 슬라이더, 왜 이 범위) 명시 없음.
- 패널 폭(320px) 결정의 디자인 기준 없음.
- Apply to 세그먼트의 `Scene` / `All` 옵션이 어떤 범위를 의미하는지 명시 없음.

---

### 1.4 SHELL — 앱 셸 / 상단바 / 페이지 라우팅

> **개요**: 모든 페이지가 공유하는 최상단 셸 레이어. 화면 최상단에 고정된 TopBar(브랜드 + 페이지 탭 + 브레드크럼/유틸리티)와, 그 아래의 페이지 콘텐츠 영역으로 구성된다. 사용자가 페이지를 전환하면 콘텐츠 영역만 교체되고 TopBar는 항상 동일한 위치에 유지된다. 새로고침 시에도 마지막으로 본 페이지가 자동 복원된다.
>
> **구현 위치**: `/whale-app/src/App.tsx` · `/whale-app/src/components/TopBar.tsx` · `/whale-app/src/styles/app.css`

---

#### FR-SHELL-01 — 앱 셸 레이아웃 (TopBar + Content 2단 그리드)

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/App.tsx` · `/whale-app/src/styles/app.css` |

##### 행위

**조건**: 사용자가 앱을 처음 열거나 페이지를 전환한다.

**액션**: 앱 루트 컴포넌트가 렌더링된다.

**결과**:
1. 화면 전체가 위에서 아래로 2단 그리드로 구성된다:
   - **상단**: `TopBar` 영역 (높이 48px 고정, 화면 폭 100%)
   - **하단**: `.content` 영역 (남은 높이 전부, 페이지 컴포넌트가 들어감)
2. 셸 전체 높이는 뷰포트(`100vh`)와 동일하며, 최소 600px 보장(`min-height: 600px`)된다.
3. `.content` 영역은 `overflow: hidden`으로 자체 영역 안에서만 스크롤이 일어난다 (TopBar는 스크롤되지 않는다).
4. TopBar는 콘텐츠 위에 떠 있는 형태가 아닌 그리드 행으로 자리를 차지하며, `z-index: 10`이 적용되어 동일 좌표에 겹치는 요소보다 위에 표시된다.

##### 측정 기준

- [ ] TopBar 높이가 정확히 48px (`--topbar-h`)이며 페이지 전환 시에도 변하지 않는다.
- [ ] 페이지 콘텐츠가 길어져도 TopBar는 화면 최상단에 고정 유지된다.
- [ ] 윈도우 높이가 600px 미만이어도 셸이 600px로 유지되어 콘텐츠가 잘리지 않는다.
- [ ] `.content` 내부 스크롤이 TopBar를 가리지 않는다.

##### 알려진 제약

- ❌ 사이드바(좌측 네비게이션) 미지원 — 페이지 네비게이션은 TopBar 중앙 탭에 단일 위치.
- ❌ 하단 푸터 / 상태바 미지원.
- ❌ 모바일/좁은 폭에 대한 별도 레이아웃 없음 (반응형 미지원).

##### 평가 기준 없음

- 윈도우 폭이 매우 좁아질 때 TopBar의 좌/중/우 3분할이 어떻게 축소되어야 하는지 디자인 기준 없음.

---

#### FR-SHELL-02 — 페이지 라우팅 및 localStorage 영속성

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/App.tsx` |

##### 행위

**조건 1 — 첫 방문**: 사용자가 앱을 처음 연다 (localStorage에 `whale:page` 키 없음).

**결과**: Home 페이지가 표시되고 TopBar의 [Home] 탭이 활성 상태이다.

**조건 2 — 탭 클릭으로 페이지 전환**: 사용자가 TopBar의 [Home] / [E2E] / [Library] 탭 중 하나를 클릭한다.

**결과**:
1. `.content` 영역이 즉시(트랜지션 없이) 해당 페이지 컴포넌트로 교체된다.
   - `home` → `<Home goto={setPage} />`
   - `e2e` → `<E2EPage />`
   - `library` → `<Library />`
2. TopBar의 활성 탭이 새 페이지로 변경된다 (`.active` 클래스 토글).
3. localStorage의 `whale:page` 키에 새 페이지 ID가 저장된다.
4. 브레드크럼 영역의 내용이 페이지에 맞게 갱신된다 (FR-SHELL-05 참조).

**조건 3 — 페이지 새로고침**: 사용자가 임의 페이지에서 브라우저 새로고침을 한다.

**결과**: localStorage의 `whale:page` 값을 읽어 마지막으로 본 페이지가 자동 복원된다.

**조건 4 — Home 페이지 내부 카드 클릭**: 사용자가 Home의 빠른 액션 카드를 통해 페이지를 전환한다.

**결과**: Home 컴포넌트에 주입된 `goto={setPage}` 콜백을 통해 동일하게 페이지가 전환되고 localStorage가 갱신된다.

##### 측정 기준

- [ ] 탭 클릭 시 페이지 전환에 추가 네트워크 요청이 발생하지 않는다 (SPA, 동일 번들 내부).
- [ ] 새로고침 후 마지막 페이지가 100ms 이내에 복원된다.
- [ ] 동일 페이지의 탭을 다시 클릭해도 컴포넌트가 unmount/remount 되지 않는다 (state 유지).
- [ ] localStorage의 `whale:page` 값이 `home` / `e2e` / `library` 외의 값일 경우에도 앱이 크래시하지 않는다.

##### 알려진 제약

- ❌ URL과 페이지 상태가 동기화되지 않음 — 브라우저 주소창은 항상 동일하며 뒤로가기/앞으로가기 동작 안 함.
- ❌ 딥링크 미지원 — 특정 페이지로 직접 진입할 URL 경로가 없음.
- ❌ 페이지 내부 상태(예: E2E의 비교 모드)는 localStorage 영속화되지 않음 — 페이지 ID만 저장.
- ❌ localStorage에 잘못된 값이 저장된 경우 검증/마이그레이션 로직 없음 (`as PageId` 캐스팅으로 그대로 사용).

##### 평가 기준 없음

- React Router 등 라우팅 라이브러리 도입 여부 / URL 기반 라우팅 지원 시점에 대한 기준 없음.

---

#### FR-SHELL-03 — 상단바 좌측 — 브랜드 / 버전 표시

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/components/TopBar.tsx` |

##### 행위

**조건**: 사용자가 임의 페이지에서 TopBar 좌측 영역을 본다.

**결과**:
1. 좌측 첫 요소로 7×7px 원형 점(`.dot`)이 액센트 색(현재 활성화된 `--accent` 토큰 색상)으로 표시된다.
2. 점 우측에 `WHALE` 텍스트가 monospace 폰트, 600 weight, 13px로 표시되며 자간(`letter-spacing`) 0.02em이 적용된다.
3. `WHALE` 우측에 1px 보더로 구분되어 `v0.4.2 · tuning studio` 버전/태그라인이 muted 색상, 11px, 일반 두께(400)로 표시된다.
4. 브랜드 영역은 좌측 14px 패딩 안쪽에서 시작하며, 모든 요소가 한 줄로 정렬된다 (`gap: 8px`).

##### 측정 기준

- [ ] 액센트 점 색상이 `Tweaks → Accent color` 변경 시 즉시 동일한 색으로 갱신된다.
- [ ] `WHALE` 텍스트가 monospace 폰트로 렌더링된다 (예: JetBrains Mono / Fira Code 계열).
- [ ] 버전 텍스트와 `WHALE` 사이에 1px 세로 보더 구분선이 보인다.

##### 알려진 제약

- ❌ 브랜드/로고 클릭 시 동작 없음 (Home으로 이동하는 일반적인 패턴 미적용).
- ❌ 버전 문자열(`v0.4.2 · tuning studio`)이 하드코딩되어 있음 — `package.json` 등과 자동 동기화되지 않음.
- ❌ 빌드 환경(dev/staging/prod) 표기 없음.

##### 평가 기준 없음

- 브랜드 점 색상이 페이지별로 달라져야 하는지에 대한 기준 없음.

---

#### FR-SHELL-04 — 상단바 중앙 — 페이지 네비게이션 탭 (Home / E2E / Library)

| 속성 | 값 |
|------|-----|
| **우선순위** | MUST |
| **상태** | ✅ 구현됨 |
| **구현 위치** | `/whale-app/src/components/TopBar.tsx` · `/whale-app/src/styles/app.css` |

##### 행위

**조건**: 사용자가 TopBar 중앙 영역을 본다.

**액션 1 — 진입 시 표시**:
1. 가로로 3개의 탭이 한 그룹(`.nav`, `role="tablist"`)에 묶여 표시된다: `[home] Home` / `[pipeline] E2E` / `[folder] Library`.
2. 그룹 컨테이너는 `--bg-subtle` 배경에 1px 보더, 둥근 모서리(`--r-md`), 내부 패딩 3px가 적용된다.
3. 각 탭은 12px 아이콘 + 12px 라벨 텍스트(가운데 정렬, gap 6px) 형태이며 비활성 시 muted 색이다.
4. 현재 페이지에 해당하는 탭에는 `.active` 클래스가 적용되어 패널 색 배경 + 일반 텍스트 색 + `--shadow-sm` 그림자가 보인다.

**액션 2 — 호버**:
- 비활성 탭에 마우스를 올리면 텍스트 색이 muted → 일반 본문 색으로 즉시 전환된다 (transition 120ms).
- 활성 탭은 호버 시 색 변화 없음.

**액션 3 — 클릭**:
- 비활성 탭 클릭 시 FR-SHELL-02의 페이지 전환 흐름이 동작한다.
- 활성 탭(현재 페이지) 재클릭 시에도 `setPage`가 호출되지만 상태 변경이 없어 사실상 무동작이다.

##### 측정 기준

- [ ] 3개 탭 중 정확히 1개에만 `.active` 클래스가 적용된다.
- [ ] 탭 클릭 후 50ms 이내에 활성 표시가 갱신된다.
- [ ] 비활성 → 활성 전환 시 배경/그림자 트랜지션이 끊김 없이 진행된다.
- [ ] 탭 그룹이 화면 가로 중앙에 위치한다 (TopBar 그리드 `1fr auto 1fr` 중 가운데 컬럼).

##### 알려진 제약

- ❌ 키보드 화살표(←/→)로 탭 이동 미지원 — `role="tablist"`가 선언되어 있으나 ARIA 키보드 패턴이 구현되지 않음.
- ❌ 단축키(예: `⌘1` / `⌘2` / `⌘3`)로 탭 전환 미지원 — Home의 빠른 액션 카드에는 단축키 칩이 표시되지만 실제 핸들러 없음.
- ❌ 탭 추가/순서 변경/숨김 등 사용자 정의 미지원.

##### 평가 기준 없음

- 미래에 추가될 페이지(예: Settings, Profile)가 동일 탭 그룹에 들어가는지, 별도 영역에 표시되는지에 대한 기준 없음.

---

#### FR-SHELL-05 — 상단바 우측 — 브레드크럼 · Sync · Settings · Avatar

| 속성 | 값 |
|------|-----|
| **우선순위** | SHOULD |
| **상태** | 🟡 부분 구현 (브레드크럼/아바타 정적, Sync/Settings 버튼 무동작) |
| **구현 위치** | `/whale-app/src/App.tsx` · `/whale-app/src/components/TopBar.tsx` |

##### 행위

**조건**: 사용자가 TopBar 우측 영역을 본다.

**액션 1 — 브레드크럼 표시 (페이지별 분기)**:
- **Home**: 브레드크럼 영역이 표시되지 않는다 (`crumb` prop이 null).
- **E2E**: monospace 11px, muted 색으로 `sensor-A / night-tuning / pass-3 · rev-7`이 표시된다. 마지막 세그먼트(`pass-3 · rev-7`)는 `--fg` 일반 본문 색으로 강조된다. 구분자 `/`는 `--fg-faint` 색이다.
- **Library**: 동일한 스타일로 `library / all images`가 표시되며 `all images`가 강조 색이다.

**액션 2 — Sync 버튼**:
1. clock 아이콘 + monospace 텍스트 `2m ago` 형태로 표시된다.
2. 버튼 스타일은 `btn sm ghost` (작은 사이즈 + 투명 배경).
3. 호버 시 `--bg-subtle` 배경이 적용된다.
4. 클릭 시 어떤 동작도 발생하지 않는다 (이벤트 핸들러 없음).

**액션 3 — Settings 버튼**:
1. 톱니바퀴 아이콘만 표시되는 정사각형 아이콘 버튼이다 (`btn sm ghost icon`, 13px 아이콘).
2. 호버 시 `--bg-subtle` 배경이 적용된다.
3. 클릭 시 어떤 동작도 발생하지 않는다 (이벤트 핸들러 없음).

**액션 4 — 아바타 표시**:
1. 22×22px 원형 영역에 액센트 계열 단색 배경(`oklch(0.72 0.08 220)`)이 적용된다.
2. 가운데에 흰색 monospace 10px, 600 weight로 이니셜 `JK`가 표시된다.
3. 클릭/호버 등 인터랙션이 없다 (단순 표식).

**공통 시각**:
- 우측 영역의 모든 요소는 가로로 6px 간격으로 우측 정렬된다 (`justify-content: flex-end`).

##### 측정 기준

- [ ] Home에서는 브레드크럼이 보이지 않고, E2E/Library에서는 페이지에 맞는 브레드크럼이 보인다.
- [ ] Sync 버튼의 `2m ago` 텍스트가 monospace 폰트로 렌더링된다.
- [ ] 아바타 원형이 정확히 22×22px이며 텍스트 `JK`가 가운데 정렬된다.

##### 알려진 제약

- ❌ Sync 버튼: 마지막 동기화 시각(`2m ago`)이 정적 문자열이며 실제 시간 경과를 반영하지 않음. 클릭 시 동기화 트리거 없음.
- ❌ Settings 버튼: 클릭 핸들러 없음 — 설정 패널/모달/페이지로 연결되지 않음. (Tweaks 패널은 별도 메시지 채널로만 활성화됨, FR-SHELL-06 참조)
- ❌ 아바타: 이니셜(`JK`)과 색상이 하드코딩 — 사용자 컨텍스트 / 인증 미연동, 클릭 시 메뉴 없음.
- ❌ 브레드크럼 세그먼트 클릭 시 상위 레벨로 이동하는 동작 없음 (정적 텍스트).
- ❌ 알림(notification) 영역 미지원.

##### 평가 기준 없음

- Sync / Settings / Avatar 각 항목의 클릭 시 어떤 화면이 떠야 하는지에 대한 디자인 기준 없음.

---

#### FR-SHELL-06 — Edit-mode 핸드셰이크 (Claude Design 통합용 postMessage)

| 속성 | 값 |
|------|-----|
| **우선순위** | COULD |
| **상태** | ✅ 구현됨 (Claude Design 호스트 환경에서만 의미) |
| **구현 위치** | `/whale-app/src/App.tsx` |

##### 행위

**조건**: 앱이 iframe 내부에서 부모 윈도우(Claude Design)로부터 마운트된다.

**액션 1 — 앱 마운트 시**:
1. 앱이 부모 윈도우로 `{ type: '__edit_mode_available' }` 메시지를 `postMessage`로 송신한다 (target origin `*`).
2. 부모 윈도우의 메시지(`__activate_edit_mode` / `__deactivate_edit_mode`)를 수신할 리스너를 `window`에 등록한다.

**액션 2 — `__activate_edit_mode` 메시지 수신**:
- 앱 내부 `tweaksOn` 상태가 `true`가 되어 우하단에 Tweaks 패널이 표시된다 (Tweaks 패널의 상세 동작은 1.7 TWK 도메인에서 정의).

**액션 3 — `__deactivate_edit_mode` 메시지 수신**:
- `tweaksOn`이 `false`가 되어 Tweaks 패널이 숨겨진다.

**액션 4 — Tweaks 값 변경 시 부모로 송신**:
- 사용자가 Tweaks 패널에서 값(예: accent, density)을 변경하면 앱이 `{ type: '__edit_mode_set_keys', edits: { [key]: value } }` 형태로 부모에 전송한다.

**액션 5 — 언마운트 시**:
- 등록된 message 리스너가 정리(`removeEventListener`)된다.

##### 측정 기준

- [ ] 앱 첫 마운트 직후 단 한 번 `__edit_mode_available`이 전송된다.
- [ ] 활성/비활성 메시지에 의해 Tweaks 패널 가시성이 즉시 토글된다.
- [ ] 부모 윈도우가 없는 일반 브라우저 환경에서도 앱이 정상 동작한다 (메시지 송신은 무시됨).

##### 알려진 제약

- ❌ `postMessage`의 target origin이 `*`로 와일드카드 — 원본 검증(origin allowlist) 없음. Claude Design 통합 외 환경에서 보안 위험 가능.
- ❌ 일반 사용자 환경(브라우저 직접 접속)에서는 Tweaks 패널을 활성화할 UI 진입점이 없음 — 메시지 채널 외에는 토글 불가.
- ❌ 메시지 페이로드 검증 / 스키마 정의 없음 (`e.data.type` 문자열 비교만).

##### 평가 기준 없음

- 일반 사용자에게 Tweaks 패널 진입점(예: Settings 버튼)을 제공할지에 대한 기준 없음.

---

### 1.5 (이후 도메인) — 작성 예정

- 1.5 TWK — Tweaks 패널 (Accent / Density / monoMeta)
- 1.6 (검토) CMP / TUN — 비교 뷰어 · 튜닝 패널은 현재 E2E 도메인에 통합되어 있어 별도 도메인 분리 여부 추가 검토 필요

---

## 2. 비기능 요구사항 (NFR) — 작성 예정
